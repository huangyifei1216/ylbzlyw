# 一两步 V5.1.3｜家庭行动陪伴工具 PRD

## 1. 产品定位

一两步是《战略养娃》IMA 分龄知识库的配套行动工具，不重复做知识问答。

- IMA：回答“为什么、现在怎么办、怎么说、观察什么”。
- 一两步：把答案变成一家人 3 或 7 天内能真正尝试的行动。
- 核心价值：家长不再只收藏方法，而是和孩子各走一小步。

主用户是家长。孩子不注册账号，家长和孩子一起商量，家长负责记录。

## 2. 唯一核心闭环

孩子档案 → 选择一个具体问题 → 建立家庭约定 → 孩子一步＋家长一步 → 家长记录双方行动 → 获得象果 → 小象步步成长／兑换家庭心愿 → 3/7 天回顾 → 继续、简化、换方向或暂停。

产品首页必须直接展示当前约定和双方今天的行动，不以知识卡、随机建议或 IMA 入口作为主功能。

## 3. 用户与权益

### 首次进入

1. 输入开通码。
2. 说明 IMA 与一两步的分工。
3. 输入孩子家庭昵称和生日。
4. 根据生日自动匹配 0—18 岁六个阶段。
5. 直接进入“建立第一个家庭约定”。

全年龄版最多 3 个孩子；单阶段版 1 个孩子。昵称和生日只保存在当前浏览器，禁止提示用户填写真实姓名、学校或住址。

## 4. 建立家庭约定

### 步骤一：选具体问题

根据年龄展示 3—4 个高频问题，也可自定义。一次只能选一个问题。

示例：`早上出门总磨蹭`、`写作业总要一直催`、`手机问题一说就吵`。

### 步骤二：双方各走一步

- 孩子这一步：今天能看到的一个小动作。
- 家长这一步：家长可以独立完成，不能写成“催孩子做到”。

0—1.5 岁不向宝宝布置任务，“宝宝这一步”记录自然出现的信号或回应。

### 步骤三：周期和家庭心愿

- 周期：3 天或 7 天。
- 家庭心愿是孩子维度的独立数据，不随单轮约定结束。已有心愿时，新约定继续为它积累象果。
- 家庭心愿：全家电影、一起做饭、公园时间、家庭游戏、小出游或 2—30 字自定义体验。
- 心愿是全家共同体验，不是孩子服从后的奖品。

### 步骤四：一起确认

展示问题、双方动作、周期、心愿和所需象果。主按钮：`就从今天开始`。

每个孩子同时只能有一个进行中的约定。新约定开始前，旧约定必须进入回顾或收进历史。

## 5. 今天页

今天页从上到下固定为：

1. 顶栏与孩子切换。
2. 当前问题和约定周期。
3. 孩子这一步及记录按钮。
4. 家长这一步及记录按钮。
5. 今天／本轮象果反馈。
6. 64—80px 的轻量步步成长摘要。
7. 家庭心愿进度。
8. 次级入口：去分龄手册继续查、查看完整约定。

0—1.5 岁按钮改为 `我观察到宝宝的信号了`；12—18 岁可按内容改为 `这周聊过啦`，避免监督感。

## 6. 象果规则

- 记录孩子这一步：1 颗。
- 记录家长这一步：1 颗。
- 0—12 岁按本地日期记录，同一天双方都记录额外 1 颗同行象果，每天最多 3 颗。
- 12—18 岁按整个 3/7 天周期记录，双方不要求同一天；每轮最多 3 颗，第二天不能重复领取。
- 10 分钟内允许撤回误操作；撤回一方时同行象果同步撤回。象果已用于尚未取消的心愿安排时，不允许撤回到负余额。
- 已撤回的记录和象果流水永久保留为历史；同周期重新记录会生成新的唯一记录与流水 ID，不覆盖旧数据。双方重新成立后，可再次获得一颗新的同行象果。
- 只有 `active` 约定可以产生双方记录和象果；`review-due`、`reviewed`、`paused`、`archived` 在领域层全部拒绝记录，不能只依赖按钮隐藏。
- 无扣分、惩罚、连续天数、排名和失败标签。

两套数值分开：

- 累计象果：决定步步成长，达到过的阶段永不倒退。
- 可用象果：累计获得减去已兑换，可用于实现家庭心愿。

多孩子家庭中，每个孩子拥有独立的象果、步步成长和心愿记录，切换孩子时不能混用余额。

## 7. 步步成长

五个阶段：

1. 0 颗：步步来到家里。
2. 6 颗：会用鼻子打招呼。
3. 18 颗：会喷出小水花。
4. 36 颗：背上探险包。
5. 60 颗：住进成长小屋。

升级时弹出可爱的成长反馈。步步页必须明确解释象果来源、下一阶段和当前家庭心愿，不能让用户猜“宠物怎么长大”。

## 8. 家庭心愿

可用象果达到所需数量后，按钮变为 `一起安排`。确认安排后扣除可用象果，累计象果和步步阶段不变，但心愿还没有进入家庭回忆。

真实活动发生后，家长点击 `这个心愿实现啦`，此时才进入家庭回忆。

- `取消这次安排`：全额返还象果，心愿回到 `active`，标题和目标保留，可再次安排。
- `放下这个心愿／换一个家庭心愿`：二次确认后状态变成 `cancelled`；若已经安排则先退款，之后可创建新心愿。
- 已完成心愿不得退款、取消或放下。

已实现心愿进入家庭回忆。

## 9. 周期回顾

3/7 天结束后首页进入回顾状态。只问：`这一轮，什么对你们有用？`

四个选择：

- 有一点变化，再试一轮。
- 动作有点难，改简单一些。
- 这个方向不合适，换个办法。
- 这段时间先放一放。

不统计缺了几天，不显示完成率，不出现通过、失败或中断。

## 10. 页面与导航

底部四栏：

- 今天：当前约定和双方行动。
- 约定：进行中约定、每日双方记录、过去轮次。
- 步步：成长阶段、象果规则、家庭心愿和回忆。
- 我的：孩子档案、购买权益、家庭备份导出/导入、隐私和重置。

IMA 只作为“方法需要调整？”的次级外链，不能写“从 IMA 答案创建约定”等内部产品语言。

## 11. 状态与边界

必须具备：未开通、首次建档、无约定、建约定四步、进行中、单方已记录、双方已记录、成长升级、象果不足、心愿可安排、心愿已安排未实现、心愿已实现、取消退款、周期结束、历史记录、阶段无权限、孩子成年、删除确认、备份导入预览和本地保存失败。

自定义问题会做最低限度的关键词保护。医疗、心理危机、发育评估或家庭安全问题进入独立边界页，不进入动作编辑、不产生象果；该保护不是诊断系统。

## 12. 数据与本地备份

- 数据合同为 V2：约定、双方记录、象果流水、独立心愿和步步历史峰值分别保存。回顾时间正式字段统一为 `review.reviewedAt`；旧版 `review.date` 只在迁移时读取。
- V1 的 `checkins` 与 `redemptions` 自动迁移且可重复执行，不重复发放或扣除象果。
- 钱包余额只由象果流水计算，不另存可任意修改的余额。
- 备份只包含家庭数据，不包含权益、开通码、支付信息或分龄手册链接。
- 规范化之后必须通过 `state-invariants.mjs`：唯一 ID、引用、日期、周期、进行中约定/心愿互斥、有效记录唯一、流水正负号、撤回/退款引用和非负余额都成立。
- 孩子、约定与心愿创建时间必须是有效 ISO 时间；行动记录、象果流水、同行象果、周期回顾、心愿支出/退款/完成/取消必须满足真实先后关系。象果余额按“时间＋原始顺序”逐笔验证，任何历史时点都不得透支。
- 心愿图标只能来自产品固定白名单；自定义心愿固定使用 `✨`。备份中的任意 HTML/CSS/脚本式图标直接拒绝，页面输出仍必须统一转义。
- 导入必须先开通权益、显示预览、执行严格结构与业务不变量校验，再由用户确认替换；失败时原数据保持不变，不静默删除、截断或修正非法业务数据。
- 本机发现非法历史状态时隔离该状态并回到空家庭数据；不会把非法数据继续当作有效状态运行。
- 本地写入和清空均为可验证事务；失败时 UI 回滚到最后一次真实持久化快照。多个同源页面通过 storage 事件同步，并用写前快照比较阻止陈旧页面静默覆盖新数据。

## 13. 视觉原则

- 家长操作层以暖米白、森林墨绿、鼠尾草绿为主；普通卡片使用细边框、圆角和轻阴影。
- 步步陪伴层局部保留步步蓝和象果黄，不占据今天页首屏。
- 页面使用同一角色母版衍生的透明图片，不再使用临时 SVG 或 CSS 裁切整张角色表。
- 12—18 岁降低角色面积和庆祝感，使用“这一轮、聊过了、有推进”等表达。
- 生产素材为九张 1024×1024 透明 WebP；头像、今天、象果、休息和五阶段成长各有独立用途。高分辨率 PNG 与角色表只放在 `design-source/`。

## 14. 运行环境与外链配置

- `config.mjs` 是本轮唯一运行配置入口，支持 `development` 与 `production`。
- development 可显示测试码、演示入口和调试说明；production 必须隐藏测试码、演示按钮和 seedDemo 入口，只说明当前为项目内测。
- 分龄手册 URL 未配置时只提供“复制当前问题”，不打开空窗口，并说明正式版本再配置入口。
- URL 已配置时，点击必须先同步打开入口，再尝试复制当前真实问题；复制失败不能阻止跳转。
- 当前版本不具备生产订单校验、支付、登录、云同步或跨设备自动恢复，页面和文档不得声称已具备。
- 正式部署必须运行 `npm run build:production`，只发布白名单中的 26 个运行文件；测试码模块、测试、PRD、审查资料和设计源图不得进入生产目录。

## 15. 验收条件

1. 新用户无需说明即可完成开通、孩子建档和第一个约定。
2. 首页 5 秒内能看懂“孩子做什么、家长做什么、点哪里记录”。
3. 双方记录后象果按 1＋1＋1 计算且刷新后保留。
4. 步步成长阶段可解释、达到后不倒退。
5. 象果可兑换家庭心愿，兑换不影响累计成长。
6. 六个年龄阶段均有具体问题和双方动作；0—1.5 岁无任务化表达。
7. 约定结束后可完成四选一回顾并进入下一轮或历史。
8. 全流程不出现惩罚、扣分、排名、连续打卡、失败标签和内部 IMA 操作术语。
9. 375×667 首屏同时看见当前问题、孩子动作与按钮、家长动作与按钮；无横向滚动或底栏遮挡。
10. 刷新、切换孩子和重新进入后数据保持一致。
11. 心愿“安排”和“真正实现”严格分开；取消安排退款后心愿仍保留，放下心愿是独立操作。
12. 本地备份可预览导入，非法数据直接拒绝且不能改变当前权益或当前家庭数据。
13. 记录撤回后可用新 ID 重新记录，历史记录和历史流水不被覆盖。
14. 同一孩子不可能同时存在两个进行中约定或两个进行中心愿。
15. 选择 3 天后切换或自定义心愿，周期和输入内容不回退；跨年龄阶段回顾后不会复制旧阶段模板。
16. 五阶段成长素材语义对应且哈希不同，九张生产 WebP 总量不超过 1.5MB。
17. production 不暴露测试码和演示入口；三种移动端尺寸无横向滚动或底栏遮挡。
18. `npm test`、`npm run test:e2e`、`npm run verify` 与 GitHub Actions 全部通过。
19. 恶意心愿图标备份被数据边界拒绝，页面不会生成攻击者控制的 style、script 或 textarea 节点。
20. 清空失败时家庭资料和当前页面均保留；成功清空后旧快照不能被后续失败写入复活。
21. 同源多页面更新可实时同步，陈旧写入不得静默覆盖；生产白名单构建不包含测试码、文档、测试或设计源文件。
