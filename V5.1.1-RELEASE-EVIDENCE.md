# 一两步 V5.1.1｜发布证据

验证日期：2026-08-25（Asia/Shanghai）

目标分支：`dev`

基线：`1608821`
范围：本地状态机、正式步步素材、移动端 H5 与自动验证；不包含 Cloudflare、支付、登录、云同步或 IMA API。

## 1. 文件修改清单

- `agreement-engine.mjs`：统一回顾字段为 `outcome/outcomeLabel/outcomeIcon/reviewedAt`。
- `fruit-ledger.mjs`：仅 active 可记录；撤回后使用 attempt ID 重记；同行象果与当前双方有效记录关联。
- `wish-engine.mjs`：拆分“取消这次安排”与“放下心愿”，支持退款后再次安排且流水 ID 不冲突。
- `state-invariants.mjs`：新增孩子、约定、双方记录、象果流水、家庭心愿的业务不变量验证。
- `data-contract.mjs`：V1→V2 后验证不变量；迁移旧 `review.date`；隔离非法本机状态；严格 V2 不静默丢弃业务数据。
- `backup.mjs`：解析和正式导入前做严格结构与状态校验；失败不改变当前数据和权益。
- `app.js`：保持第三步草稿、跨阶段重选、心愿双语义、production 隔离、手册同步打开、青少年文案和 WebP 素材。
- `config.mjs`：提供 development/production 与 `handbookUrl` 运行配置。
- `assets/bubu-*.webp`：九张 1024×1024 透明生产素材。
- `design-source/`：母版、角色表和高分辨率源图；生产 HTML/CSS/JS 不引用。
- `styles.css`、`styles-brand.css`：基础结构与正式品牌/响应式两层；移除临时 SVG 小象和旧版本覆盖文件名。
- `agreements.mjs`、`core.mjs`：保留当前模板与年龄规则，删除无引用 V4 钱包、随机卡和 key 逻辑。
- `tests/*.mjs`：新增状态不变量、严格备份、重记、心愿语义和素材预算测试。
- `tests/e2e/core-flow.spec.mjs`：17 条真实移动端流程，包括三尺寸矩阵、五阶段截图和图片失败降级。
- `.github/workflows/verify.yml`：Node 22 上运行 npm ci、单测和 Chromium E2E，失败上传 trace/截图/结果。
- `PRD.md`、`GPT-PRO-HANDOFF.md`、`GPT-PRO-REVIEW-INDEX.md`、`README.md`、`project-control.md`：同步 V5.1.1 规则与真实能力边界。

## 2. P0 Bug 验证

### 记录 → 撤回 → 重新记录

`tests/fruit-ledger.mjs` 证明：

1. 孩子记录获得 1 颗；
2. 撤回后余额回到 0；
3. 同日重新记录生成不同的 record ID；
4. 旧记录仍保留，新记录再次获得 1 颗；
5. 双方共 3 颗时撤回一方只剩 1 颗，重记后产生新的同行流水并恢复 3 颗；
6. 所有历史记录和流水 ID 仍唯一。

### 非 active 不能记录

同一测试直接调用领域函数：`active` 正常；`review-due`、`reviewed`、`paused`、`archived` 全部抛出 `agreement-not-active`。该规则不依赖 UI。

### 回顾、备份与互斥

- `tests/data-contract.mjs`：新版 `reviewedAt` 往返不丢失，旧 `date` 迁移，非法本机时间被隔离。
- `tests/backup.mjs`：非法 `reviewedAt`、非法金额或重复进行中约定直接拒绝，当前家庭数据不变。
- `tests/state-invariants.mjs`：两个 active、active＋review-due、两个 active 心愿、无效引用、错误金额/正负号、非法日期、重复有效记录、负余额和重复退款全部拒绝。

## 3. 本地测试结果

```text
$ npm test
9 groups passed:
Smoke / Assets / Data contract / State invariants / Agreement engine /
Fruit ledger / Wish engine / Safety / Backup

$ npm run test:e2e
17 passed

$ npm run verify
9 Node groups passed
17 Playwright tests passed
```

额外语法检查：`app.js`、五个领域/合同模块、`safety.mjs`、`state-invariants.mjs` 均通过 `node --check`。

## 4. CI

工作流：`.github/workflows/verify.yml`。成功链接在推送 `dev` 并完成 GitHub Actions 后补入本节。

## 5. 步步素材报告

全部为 1024×1024、有透明通道的 WebP；单张低于 180KB，首屏 avatar/today 低于 120KB。

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| bubu-avatar.webp | 66,254 | `3d779feb168601be602eae14e9cd8074ca55ecea0314474d26f48726dde7cb60` |
| bubu-today.webp | 77,378 | `232179b863873329d080f432286737318f39a4c38d7f5b96c64e69cd89eb3377` |
| bubu-fruit.webp | 70,320 | `9f6a311c2b413e6d7c45aebfbb1a2bdd101e2af4e4754eec6a9b90737b30b9d9` |
| bubu-rest.webp | 68,762 | `ffe8a757357797db436510e25e3540b3ca7dc82ceee266e8f0aa8a03f7f0a923` |
| bubu-growth-1.webp | 63,518 | `dcfce281cbc294e11bb919458b5d0796e24733ae51db8b4ba75b1a93544edc00` |
| bubu-growth-2.webp | 65,700 | `7eb14d01fba03defe3f2ad3aad249a917e360945fdbc9d38064a2bfc4c19c578` |
| bubu-growth-3.webp | 74,270 | `9f154e55a99f0bea1faa2cd1df961eca984c804a7ee7f293e1cc3544690336eb` |
| bubu-growth-4.webp | 71,606 | `e84993fbe91eaba896a5f5d5cb3661665a6c0e03eefc062f18f962aef6a15a41` |
| bubu-growth-5.webp | 69,720 | `02380e001cf7cdbbd8710ea007ed188acdcc2b7cb6cf976f780229bab0e5682f` |

总量：627,528 bytes（612.8KB），低于 1.5MB。九张生产素材哈希全部不同；五阶段分别对应来到家里、抬鼻打招呼、喷水、探险包和成长小屋。

## 6. 浏览器截图

- `artifacts/screenshots/today-375x667.png`
- `artifacts/screenshots/teen-once-per-cycle-390x844.png`
- `artifacts/screenshots/bubu-growth-1-390x844.png` 至 `bubu-growth-5-390x844.png`
- `artifacts/screenshots/wish-unscheduled-retained-390x844.png`
- `artifacts/screenshots/backup-invalid-rejected-390x844.png`
- `artifacts/screenshots/production-activation-390x844.png`

Playwright 还在 375×667、390×844、430×932 上检查开通、建档、四步约定、今天、历史、步步、心愿、回顾、备份和敏感边界的横向溢出与底栏遮挡；375×667 第一视口内双方按钮可操作。

## 7. 能力边界与发布检查

- 当前数据默认只在浏览器 localStorage；用户主动导出/导入 JSON，不是云同步。
- production 模式隐藏测试码、演示入口和 seedDemo 入口，只说明项目内测。
- 当前没有真实订单安全、支付、登录、云同步、跨设备自动恢复或 IMA API。
- 未发现密钥或真实用户数据。
- 生产代码只引用 `assets/*.webp`；`design-source/` 不被页面引用。
- 只在 `dev` 开发，`main` 保持 `6292f2d` 未修改。

最终 `git status`、`git log --oneline -6` 和 CI 链接以推送后的本文件更新为准。
